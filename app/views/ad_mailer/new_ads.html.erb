<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
<h1>Hey, <%= @user.name %></h1>
<p>
  There are <%= @ads_number %> new apartments for you
</p>
<% @search_link_with_ads.each do |link_name, ads| %>
    <% if ads.present? %>
        <h2><%= link_name %></h2>
        <% ads.each do |ad| %>
            <p>
              <%= link_to(image_tag(ad.image_path, height: '80', width: '80') + ad.title, ad, target: '_blank') %>
            </p>
        <% end %>
    <% end %>
<% end %>

<p>Have a great day!</p>
</body>
</html>
