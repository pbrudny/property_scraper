<%= form_for(@search_link, url: site_search_link_path(@site)) do |f| %>
  <% if search_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_link.errors.count, "error") %> prohibited this search_link from being saved:</h2>

      <ul>
      <% search_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
