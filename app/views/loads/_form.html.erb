<%= simple_form_for(@load) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :errors %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
